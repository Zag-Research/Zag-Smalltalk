"
I am an expression that creates a block closure object (references a block and the current environment)
"
Class {
	#name : #ASCreateBlock,
	#superclass : #ASExpression,
	#instVars : [
		'value'
	],
	#category : #'ASTSmalltalk-AST'
}

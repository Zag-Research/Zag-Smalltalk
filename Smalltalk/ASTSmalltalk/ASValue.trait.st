"
I am a trait with methods for all expression values. This could just be methods in ASExpression except that ASBlock also is an expression value.
"
Trait {
	#name : #ASValue,
	#category : #'ASTSmalltalk-AST'
}
